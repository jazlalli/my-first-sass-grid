{
  "name": "sass-grid",
  "version": "0.0.1",
  "description": "Demo project to build a grid system from scratch",
  "main": "index.js",
  "dependencies": {
    "http-server": "^0.9.0"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "sass --watch ./sass/styles.scss:./css/styles.css",
    "build": "sass ./sass/styles.scss:./css/styles.css",
    "start": "npm run build && http-server"
  },
  "keywords": [
    "grid",
    "sass"
  ],
  "author": "Jaz Lalli",
  "license": "MIT"
}
